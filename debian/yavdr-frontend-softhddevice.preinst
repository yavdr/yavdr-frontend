#!/bin/sh -e

if [ install = "$1" -o upgrade = "$1" ]; then

  # conffiles
  . /usr/share/yavdr/helpers/conffiles
  install_conffile yavdr-frontend-softhddevice /etc/vdr/plugins/plugin.softhddevice.conf

fi

